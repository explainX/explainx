# Core runtime dependencies for explainx-llm.
numpy>=1.23
pandas>=1.5
scikit-learn>=1.1
joblib>=1.2

# Optional but recommended:
#   shap>=0.44   -> state-of-the-art SHAP attributions (auto-used if installed)
#   mcp>=1.0.0   -> run the MCP server so LLM agents can call explainx as tools
# Install everything with:  pip install -e ".[all]"
